# Visual Status Guide - What You'll See

## Status Indicators

The application now shows clear visual indicators about AWS configuration status.

### 🟢 Green Banner - "AWS Bedrock Connected"

```
┌─────────────────────────────────────────────────────────┐
│ ✓ AWS Bedrock Connected                                 │
│ Using real AWS Bedrock AI for intelligent career        │
│ guidance. [Live]                                         │
└─────────────────────────────────────────────────────────┘
```

**What this means:**

- ✅ AWS credentials are configured correctly
- ✅ All AI features use real Bedrock API
- ✅ Job scores calculated by Claude 3.5 Haiku
- ✅ Resume analysis from real AI
- ✅ Career roadmaps generated by AI
- 💰 AWS costs will apply (very minimal)

**You'll see this when:**

- `.env` file has valid AWS credentials
- `VITE_USE_MOCK_DATA=false` (or not set)
- `VITE_ENABLE_AWS_BEDROCK=true` (or not set)
- AWS Bedrock is accessible from your account

---

### 🟠 Orange Warning - "AWS Configuration Required"

```
┌─────────────────────────────────────────────────────────┐
│ ⚠ AWS Configuration Required                            │
│                                                          │
│ To use real AWS Bedrock AI features, you need to        │
│ configure your AWS credentials.                          │
│                                                          │
│ Missing Configuration:                                   │
│ • VITE_AWS_ACCESS_KEY_ID                                │
│ • VITE_AWS_SECRET_ACCESS_KEY                            │
│                                                          │
│ Setup Steps:                                             │
│ 1. Get AWS credentials from AWS Console                 │
│ 2. Ensure you have access to AWS Bedrock                │
│ 3. Create a .env file in the project root               │
│ 4. Add your credentials                                  │
│ 5. Restart the development server                       │
│                                                          │
│ [AWS Bedrock Docs] [Reload App]                         │
└─────────────────────────────────────────────────────────┘
```

**What this means:**

- ⚠️ AWS credentials are missing or invalid
- ⚠️ AI features will show errors
- ⚠️ Cannot use real Bedrock API
- ℹ️ Setup instructions provided

**You'll see this when:**

- No `.env` file exists
- `.env` file has placeholder values
- AWS credentials are invalid
- `VITE_USE_MOCK_DATA=false` but credentials missing

**What to do:**

1. Follow the setup instructions in the banner
2. See `AWS_SETUP_GUIDE.md` for detailed steps
3. Or switch to Demo Mode (see below)

---

### 🔵 Blue Banner - "Demo Mode"

```
┌─────────────────────────────────────────────────────────┐
│ ℹ Demo Mode                                              │
│ Using mock data for demonstration. Configure AWS         │
│ credentials to use real AI features. [Demo]              │
└─────────────────────────────────────────────────────────┘
```

**What this means:**

- 🔵 Using sample/mock data
- 🔵 All features work but with fake data
- 🔵 No AWS costs
- 🔵 Perfect for testing UI/UX

**You'll see this when:**

- `.env` has `VITE_USE_MOCK_DATA=true`
- Intentionally using demo mode

**Use this when:**

- Testing the UI without AWS account
- Demonstrating features without costs
- Developing frontend without backend
- Learning how the app works

---

## Feature-Specific Indicators

### Job Search Dashboard

#### With Real AWS:

```
┌─────────────────────────────────────────────────────────┐
│ 🟢 AWS Bedrock Connected                                │
└─────────────────────────────────────────────────────────┘

AI-Powered Job Search
Intelligent job matching powered by AWS Bedrock • 6 opportunities found

┌─────────────────────────────────────────────────────────┐
│ 🔄 Loading job opportunities...                         │
└─────────────────────────────────────────────────────────┘

[After loading]

┌─────────────────────────────────────────────────────────┐
│ Senior Software Engineer                                 │
│ TechCorp Inc. • San Francisco, CA                       │
│ AI Score: 94 🟢 (Calculated by Claude 3.5 Haiku)       │
└─────────────────────────────────────────────────────────┘
```

#### Without AWS:

```
┌─────────────────────────────────────────────────────────┐
│ 🟠 AWS Configuration Required                           │
│ [Setup instructions...]                                  │
└─────────────────────────────────────────────────────────┘

AI-Powered Job Search
Intelligent job matching powered by AWS Bedrock • 0 opportunities found

┌─────────────────────────────────────────────────────────┐
│ ⚠ Error Loading Jobs                                    │
│ AWS credentials not configured. Please set               │
│ VITE_AWS_ACCESS_KEY_ID and VITE_AWS_SECRET_ACCESS_KEY  │
│ in .env file                                             │
│ [Try Again]                                              │
└─────────────────────────────────────────────────────────┘
```

---

## Loading States

### Job Loading:

```
┌─────────────────────────────────────────────────────────┐
│           🔄                                             │
│     Loading job opportunities...                         │
└─────────────────────────────────────────────────────────┘
```

### AI Processing:

```
┌─────────────────────────────────────────────────────────┐
│ 🤖 AI is analyzing your resume...                       │
│ This may take a few seconds                              │
└─────────────────────────────────────────────────────────┘
```

---

## Error States

### AWS Access Denied:

```
┌─────────────────────────────────────────────────────────┐
│ ❌ AWS Access Denied                                    │
│ Please check your credentials and Bedrock permissions.   │
│ [Try Again] [View Setup Guide]                          │
└─────────────────────────────────────────────────────────┘
```

### Model Not Found:

```
┌─────────────────────────────────────────────────────────┐
│ ❌ Bedrock model not found                              │
│ Ensure you have access to Claude 3.5 Haiku in your      │
│ AWS region.                                              │
│ [Check Model Access] [Try Again]                        │
└─────────────────────────────────────────────────────────┘
```

### Network Error:

```
┌─────────────────────────────────────────────────────────┐
│ ❌ Network Error                                        │
│ Failed to connect to AWS Bedrock. Check your internet   │
│ connection and try again.                                │
│ [Retry]                                                  │
└─────────────────────────────────────────────────────────┘
```

---

## Toast Notifications

### Success:

```
✓ Loaded 6 job opportunities
```

### Error:

```
✗ Failed to load jobs: AWS credentials not configured
```

### Info:

```
ℹ Using mock data. Configure AWS for real AI features.
```

---

## Browser Console Messages

### With AWS Configured:

```
✓ AWS Bedrock configured correctly
✓ Using real AI for job scoring
✓ Loaded 6 jobs with AI compatibility scores
```

### Without AWS:

```
⚠ Using mock job data. Configure AWS credentials to use real data.
⚠ AWS Bedrock is not configured properly. Missing: VITE_AWS_ACCESS_KEY_ID, VITE_AWS_SECRET_ACCESS_KEY
```

### In Demo Mode:

```
ℹ Demo mode enabled. Using mock data for all features.
```

---

## Quick Reference

| Status                 | Color     | Meaning         | Action Needed       |
| ---------------------- | --------- | --------------- | ------------------- |
| Connected              | 🟢 Green  | AWS working     | None - enjoy!       |
| Configuration Required | 🟠 Orange | AWS not setup   | Follow setup guide  |
| Demo Mode              | 🔵 Blue   | Using mock data | Optional: add AWS   |
| Error                  | 🔴 Red    | Something wrong | Check error message |
| Loading                | ⚪ Gray   | Processing      | Wait a moment       |

---

## What Each Status Means for Features

| Feature         | 🟢 Connected      | 🟠 Not Configured | 🔵 Demo Mode   |
| --------------- | ----------------- | ----------------- | -------------- |
| Job Scoring     | Real AI scores    | Error shown       | Mock scores    |
| Resume Analysis | Real AI analysis  | Error shown       | Mock analysis  |
| Career Roadmap  | Real AI roadmap   | Error shown       | Mock roadmap   |
| Market Insights | Real AI insights  | Error shown       | Mock insights  |
| Interview Prep  | Real AI questions | Error shown       | Mock questions |

---

## Tips

1. **First Time Setup:** You'll see the orange warning - this is normal!
2. **After Configuration:** Restart the dev server to see the green banner
3. **Testing Without AWS:** Set `VITE_USE_MOCK_DATA=true` for demo mode
4. **Troubleshooting:** Check browser console (F12) for detailed errors
5. **Cost Concerns:** Demo mode is free, real AWS costs ~$0.10-0.50/day testing

---

**Visual feedback makes it easy to know exactly what's happening!** 👀
